#include <iostream>
#include "headers/verify.hpp"
#include "headers/currency.hpp"

void checkCurrency(const std::string currency, const double numOne, const double numTwo) {
	static int temp{0};
	if (temp == 0) {
		temp ++;
	} else {

	}
}